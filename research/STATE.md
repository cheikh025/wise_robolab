# Current Research State

Status: `IN_PROGRESS`

Current milestone: `M4_IDM_TRAIN` (M0, M1, M2, M3 passed)

Working repo: `https://github.com/cheikh025/RoboLab` @ `/workspace/RoboLab`, branch `wise/m0-setup`, commit `74d3090` (WISE Best-of-K selector)

Upstream: `https://github.com/NVlabs/RoboLab` @ `97bc1e766300a8c95656067ecb81e267e7e11823`

Accepted implementation/checkpoint state: `UNSET`

Measured baselines: Cosmos3-Edge B=1 on `BananaInBowlTask`: 2/2 episodes succeeded (score=1.0 each, 155 and 141 steps respectively). Small-n, not a confirmation-grade result — broader panels deferred to M7.

Robometer status: validated offline (M2 passed). All 3 real views show clean monotonic progress correlating with the actual grasp/success timeline; 2/3 dream views match closely, 1 anomaly (dream-right collapsed on the final chunk of one episode — flagged for M5). Required env fix: `torchao<0.10` (transitive dep, not pinned upstream, re-apply after fresh syncs).

IDM status: implemented and trained at 500-episode scale (RUN_0009 passed; `/workspace/wise_idm` now a git repo, commit `58b3160`). `DroidIDM` (SmallCNNBackbone, no VAE roundtrip per explicit user constraint) takes the full 33-frame Cosmos dream (no subsampling) across 3 cameras and predicts the 32-action chunk (7 joints + gripper). Best checkpoint (epoch 30/40, `/workspace/wise_idm/checkpoints_500ep/best.pt`): val_mean_joint_mae=0.06857, val_gripper_accuracy=0.98685, save/reload verified exact. ~7.5x improvement over the 27-episode debug baseline (RUN_0008). Architecture decision (RUN_0010, completed): a wide/deep variant (23.8M params) was compared against the baseline (9.0M params) on identical 500-episode data — baseline won by ~17% relative (val_mean_joint_mae 0.0686 vs 0.0803), showing this IDM is currently data-limited, not capacity-limited. Baseline architecture (cnn_width=64, d_model=256, 4+4 layers) is the carried-forward reference config. Found/fixed a reproducibility bug in the process: checkpoint `config` dicts omitted architecture hyperparameters — fixed in `train.py`/`verify_checkpoint.py` (commit `8687125`) and patched into both existing checkpoints. 5000-episode download completed; first attempt at 5000ep training (RUN_0012/RUN_0013, 15 epochs, two batch/lr settings) underperformed the 500ep checkpoint (~0.124-0.126 vs 0.06857) — diagnosed as an epoch-budget problem (15 passes over 5000ep episodes vs RUN_0009's 40 passes over 500ep, despite ~3x more total gradient steps: an underfitting signature). Added multi-GPU DDP support to `train.py` (commit `fea5f54`, also fixed a wasteful full-video-decode pass in stats computation) so both A100s can train one job together instead of splitting into parallel single-GPU experiments. **RUN_0015 (completed)**: baseline architecture, 5000 episodes, 30 epochs, both GPUs via `torchrun`/DDP — best result among all 5000ep attempts (val_mean_joint_mae=0.11701, epoch 21/30) and epoch budget confirmed as a real, fixable factor (beat RUN_0012/13's 15-epoch results), but **still worse than RUN_0009's 500-episode checkpoint (0.06857)** even with proper convergence (flat for the last 8 epochs). This is a genuine capacity/token-efficiency finding, not under-training. **RUN_0009's 500-episode checkpoint remains the best available IDM.** This motivated a full architecture redesign (discussed at length, not yet implemented): replace the current flatten-to-spatial-grid encoding (1537 tokens: 32 pairs x 3 cameras x 16 grid-cells) with spatial-softmax token compression (96 tokens: one per camera-pair, ~256x cheaper attention, per EVA's validated spatial-softmax-beats-flatten ablation and VPT's per-frame-token-compression trick), drop the proprioception token (pure vision, matching EVA/VPT precedent), keep everything else (CNN backbone, adjacent-pair fusion, global non-causal transformer, learned-query joint decoder, joint SmoothL1 + gripper BCE+pos_weight heads) unchanged. Deliberately did *not* add VPT's wider local-temporal-conv on top of this -- reasoned that pair-token-level global attention already gives a comparable path to cross-step motion info. Plan: (1) quick M5 task test using RUN_0009's checkpoint on real saved Cosmos dream video; (2) implement the v2 architecture; (3) train it at 5000-episode scale with careful GPU batch sizing (check `nvidia-smi` memory headroom before committing, per the earlier RUN_0015 lesson); (4) push **both** the current-architecture (RUN_0009) and new-architecture checkpoints to HF for comparison, per explicit user direction. Not yet done: failure-trajectory inclusion (user-suggested, still open).

Full WISE status: not integrated (M6). `r_task` (Robometer, M2/M3) and `r_exec` (action validity/temporal regularity, RUN_0011, no hard-reject per explicit user direction) are both implemented and validated standalone. `r_cons` (IDM-vs-Cosmos consistency) needs the IDM validated on real dream videos first (M5) before it can be computed. Fusion method (naive weighted z-score sum per METHOD.md) flagged by user as a lower-priority thing to improve at M6 — see `research/NOTES.md`.

Machine notes: unprivileged Vast.ai container, no sudo (use local provisioner API for root actions — see RUNBOOK.md), `/workspace` is not a persistent volume. HF authenticated as `cheikh025`; access confirmed for `nvidia/Cosmos3-Edge-Policy-DROID`, `nvidia/Cosmos-Guardrail1` (gated=auto), `robometer/Robometer-4B`.

cosmos-framework status: verified end-to-end (M1 passed). Server up on `localhost:8000` with `nvidia/Cosmos3-Edge-Policy-DROID`, `--decode-video` enabled, dream-video retention patched into RoboLab's client (commit `16513ca`). Same-context K=5 diversity confirmed real (no collapse). Server process left running for M2's dream-scoring input.

Best-of-K status: implemented and validated (M3 passed). `policies/wise/BestOfKClient` at K=3 on `BananaInBowlTask`: selector correctness verified exactly (robometer=argmax 5/5, random=stochastic 2/6 argmax matches, first=always idx0 8/8); all 3 episodes succeeded, step-efficiency ordering robometer(134)<random(182)<first(253) is suggestive at n=1/arm, not confirmatory. Robometer now also runs as a persistent HTTP scoring server (`research/tools/robometer_server.py`, port 8100) for cross-process calls from RoboLab.

Immediate next action: monitor the 5000-episode download to completion; once ready, retrain the baseline architecture at 5000-episode scale and compare against the 500ep checkpoint. In parallel/afterward, proceed to M5 (IDM validation on held-out data + real Cosmos dream inference), then M6 (full WISE integration: fuse r_exec + r_cons + r_task).

Keep this file compact. Put detailed history in `research/progress/` and `research/EXPERIMENTS.jsonl`.
